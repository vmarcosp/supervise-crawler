(executable
   (name App) 
   (public_name app.exe)
   (libraries  lambdasoup cohttp cohttp-lwt cohttp-lwt-unix lwt lwt_ppx pastel.lib console.lib yojson )
   (preprocess ( pps lwt_ppx ))
)
(include_subdirs unqualified)
